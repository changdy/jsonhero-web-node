// Shim for @jsonhero/json-infer-types/lib/formats
// This module only contains TypeScript type declarations with no runtime code.
// All types are available from the main package entry in TypeScript via .d.ts files.
// This shim allows the bundler to resolve the import at runtime (returns empty object).
export default {};
export {};
